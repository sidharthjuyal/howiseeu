// Preload the audio fileconst rejoiceSound =document.getElementById('rejoiceSound');rejoiceSound.preload = 'auto'; // Preload the audio// Function to play rejoice soundfunction playRejoiceSound() {rejoiceSound.currentTime = 0; // Reset audio to startrejoiceSound.play(); // Play the audio}// Function to trigger confettifunction triggerConfetti() {confetti({particleCount: 700,spread: 500,origin: { y: 0.5 }});}// Function to check if all tiles are flippedfunction checkAllTilesFlipped() {const gridItems =document.querySelectorAll('.grid-item');const allFlipped = Array.from(gridItems).every(item => {return item.classList.contains('flipped');});if (allFlipped) {playRejoiceSound();triggerConfetti();}}// Add event listener to each tiledocument.querySelectorAll('.grid-item').forEach(item => {item.addEventListener('click', () => {item.classList.toggle('flipped');checkAllTilesFlipped();});});
